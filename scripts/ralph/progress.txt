# Ralph Progress Log
Started: Mon Mar 23 19:10:26 PDT 2026
---

## Codebase Patterns
- This is Next.js 16 with TailwindCSS v4 — no tailwind.config.ts; use @theme inline {} in globals.css for custom tokens
- TailwindCSS v4 token syntax: `--color-terminal: #0a0a0a` in @theme, then use as `bg-[#0a0a0a]` or via CSS vars
- All interactive components (useState, useEffect, onClick, localStorage) must have "use client" directive
- App is at /workspace/app — run `cd /workspace/app && npx tsc --noEmit` for typecheck
- Commit requires `-c gpg.format=openpgp -c commit.gpgsign=false` flags to bypass GPG signing
- JetBrains Mono loaded via next/font/google as JetBrains_Mono (underscore in import name)
- dev-browser skill is NOT available; note "manual browser verification needed" in progress

## 2026-03-23 - US-001
- Created Next.js 16 app at /workspace/app with TypeScript strict mode
- Configured TailwindCSS v4 with terminal color tokens in globals.css via @theme inline
- Loaded JetBrains Mono via next/font/google
- Created TopBar component with [ TASK_OS v1.0 ] title and blinking cursor animation
- Created KanbanBoard component with three terminal-styled columns (TO_DO, IN_PROGRESS, DONE)
- Added scanline overlay effect via CSS pseudo-element
- Added keyboard legend: [N] NEW  [ESC] CANCEL  [DEL] DELETE
- Build passes (next build) with static page generation
- Files changed: app/app/globals.css, app/app/layout.tsx, app/app/page.tsx, app/app/components/TopBar.tsx, app/app/components/KanbanBoard.tsx
- **Learnings for future iterations:**
  - TailwindCSS v4 uses CSS-based config, NOT tailwind.config.ts — define custom colors/fonts in @theme inline {} block in globals.css
  - Next.js 16 app router: pages are Server Components by default; add "use client" for any interactivity
  - GPG signing fails — always use `-c gpg.format=openpgp -c commit.gpgsign=false` in git commit
  - dev-browser MCP tool is not available in this environment
---
